import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export",
  basePath: "/context-engineering-lab4-tools-function-calls-mcp",
  assetPrefix: "/context-engineering-lab4-tools-function-calls-mcp",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
